# T8 split config. `ratios` allocates speakers within each (accent_label, source) stratum;
# any stratum with ≥3 speakers is guaranteed at least one speaker in train, val and test
# (a 4-speaker gold stratum → 2/1/1).
ratios: [0.8, 0.1, 0.1]
seed: 17
