# Frozen weak-labeling config (decision #2, guards against circular reasoning).
# `revision` must be pinned hard before the first run:
#   uv run python -c "from huggingface_hub import HfApi; \
#     print(HfApi().model_info('Qwen/Qwen2-Audio-7B-Instruct').sha)"
# Paste the sha below, and record the pin date in the datasheet.
# The same config is shared by weak labeling (T13) and the zero-shot baseline (T11) — that
# dual role has to be disclosed in the report.
model_id: Qwen/Qwen2-Audio-7B-Instruct
revision: PIN_ME              # replace with the commit sha before the first run; never leave PIN_ME
prompt_file: prompts/qwen2audio_accent_v1.txt
prompt_sha256: 2d3ea2e15006b9401a56f1be1a815f42be08513d42d46f1a3e8c63dfacfbddf3
k_votes: 3                    # self-consistency votes; stop-loss ladder ② can drop this to 1 (rule unchanged)
temperature: 0.7
kill_precision: 0.80          # if audited precision falls below this, drop that class's weak labels entirely
