# Common Voice English — primary path is HF common_voice_17_0 (CC0; gated, but the gate is
# a click-through).
# Note: since 2025-10 official distribution runs exclusively through the Mozilla Data
# Collective, so v17 is the last release on HF. MDC v26 is an optional upgrade — once the
# registration clears, just point `root` at it.
source: common_voice
license: CC0-1.0
source_uri: hf://mozilla-foundation/common_voice_17_0
root: data/raw/common_voice/en
tsv: validated.tsv
clips_dir: clips
